<%= form_for(@comment, class: "form") do |f| %>
  <div class="form-element">
    <%= f.label :name %>
    <%= f.text_field :name, autofocus: true, autocomplete: "name", class: "field_group" %>
  </div>
  <div class="form-element">
  <%= f.label :email %>
  <%= f.email_field :email, autocomplete: "email", class: "field_group" %>
  </div>
  <div class="form-element">
  <%= f.text_area :body, autocomplete: "comment", class: "field_group" %>
  </div>
  <div class="form-element">
  <%= f.submit "Send" %>
  </div>
  <div class="icon-comment">
    <i class="fas fa-comments"></i>
  </div>
<% end %>
